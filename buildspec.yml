version: 0.2

runtime-versions:
  nodejs: 20.x

phases:
  install:
    commands:
      - npm install

  build:
    commands:
      - npm run build

  post_build:
    commands:
      - npm run export

artifacts:
  files:
    - ".next/**/*"
    - "out/**/*"
  discard-paths: yes
